// Exportando a função (export default)
export default function subtract(x, y) {
  return x - y;
}

/*
    EXPORT DEFAULT: 
    - É usado para declarar um valor de fallback (recurso)
    - Só se pode ter UM valor como export default em cada módulo ou arquivo
    - NÃO se pode usar export default com var, let ou const
*/


