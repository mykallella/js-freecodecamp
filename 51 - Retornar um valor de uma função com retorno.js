function timesFive(num) {
  return num *= 5;  // Retorna multiplicação de n por 5
}

console.log(timesFive(3)) // Imprime retorno da função

let soma = timesFive(3) + 5 // Retorno da função + 5

console.log(soma) // Imprime var soma

