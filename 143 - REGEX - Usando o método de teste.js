// Regex (Expressões Regulares)

let myString = "Hello, World!";
let myRegex = /Hello/; // Procura Hello
let result = myRegex.test(myString); 
// A string de myRegex é procurada na string entre parênteses
// Se achar retorna 'true', se não 'false'

console.log(result)

/*
    REGEX.test(STRING)
*/

