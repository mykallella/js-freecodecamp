const myArray = [["John", 23], ["cat", 2]];

let removedFromMyArray = myArray.pop(); // Remove último elemento e retorna o mesmo

console.log(removedFromMyArray);

/*
.pop() remove o último elemento de uma matriz e retorna esse elemento.
*/

